<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ingzone.dao.ProjectDao">

    <resultMap id="Project" type="com.ingzone.model.dto.Project">
        <id column="id" property="id"/>
        <result column="introduce" property="introduce"/>
        <result column="github" property="github"/>
        <result column="date" property="date"/>
        <result column="game" property="game"/>
    </resultMap>

    <select id="getProject" parameterType="map" resultMap="Project">
        SELECT * FROM project LIMIT #{begin}, #{rows}
    </select>

    <select id="getProjectCount" resultType="int">
        SELECT COUNT(*) FROM project;
    </select>
</mapper>